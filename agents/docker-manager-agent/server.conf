# docker-manager-agent server config. Restart required for changes.
HOST=0.0.0.0
PORT=8889
LOG_LEVEL=info

# Number of times to ring the terminal bell when user intervention is required.
# Change this value at any time — it is read at each alert, no restart needed.
BELL_RINGS=5
